export type ThemeConfigCalandar = 
{
        /** CSS class dark theme, to change calandar theme */
    darkModeClass?: string,

    /** CSS class light theme, to change calandar theme */
    lightModeClass?: string;
    defaultTheme?: 'light' | 'dark';
}